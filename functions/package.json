{
  "name": "scaffolding",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "moment-range": "^4.0.2",
    "moment-timezone": "^0.5.33"
  }
}
